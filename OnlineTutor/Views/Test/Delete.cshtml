@model OnlineTutor.Models.Test

@{
    ViewData["Title"] = "Удаление теста";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h4>Удаление теста</h4>
                </div>
                <div class="card-body">
                    <h5>Вы действительно хотите удалить этот тест?</h5>

                    <div class="alert alert-warning mt-3">
                        <p><strong>Внимание!</strong> Удаление теста приведет к удалению всех связанных вопросов и результатов.</p>
                    </div>

                    <dl class="row mt-4">
                        <dt class="col-sm-3">Название:</dt>
                        <dd class="col-sm-9">@Model.Title</dd>

                        <dt class="col-sm-3">Предмет:</dt>
                        <dd class="col-sm-9">@Model.Subject</dd>

                        <dt class="col-sm-3">Тема:</dt>
                        <dd class="col-sm-9">@(Model.Topic ?? "-")</dd>

                        <dt class="col-sm-3">Дата создания:</dt>
                        <dd class="col-sm-9">@Model.CreatedDate.ToString("dd.MM.yyyy")</dd>

                        <dt class="col-sm-3">Количество вопросов:</dt>
                        <dd class="col-sm-9">@Model.Questions.Count</dd>
                    </dl>

                    <form asp-action="Delete" method="post" class="mt-4">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-secondary me-md-2">Отмена</a>
                            <button type="submit" class="btn btn-danger">Удалить</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
