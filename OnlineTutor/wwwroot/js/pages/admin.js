/**
 * Скрипты для страниц администратора
 */

const AdminPage = {
    init: function () {
        // Инициализация функций для страниц администратора
        console.log('Инициализация страницы администратора');
    }
};

// Инициализация при загрузке документа
$(document).ready(function () {
    AdminPage.init();
});
